<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:widgets="clr-namespace:Tasker.Views.Widgets"
             x:Class="Tasker.Views.TaskHome"
             Title="{Binding Title}">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="0,5">
                <Button Text=" + Task" HorizontalOptions="Center" CornerRadius="50" Command="{Binding NewTaskCreateCommand}" />
                <widgets:TaskGroup Items="{Binding Running}" TaskStatusChange="OnTaskStatusChange" GroupTitle="Running" IsExpand="True" />
                <widgets:TaskGroup Items="{Binding Paused}" TaskStatusChange="OnTaskStatusChange" GroupTitle="Paused" />
                <widgets:TaskGroup Items="{Binding ReadyToStart}" TaskStatusChange="OnTaskStatusChange" GroupTitle="Ready to Start" />
            </StackLayout>
        </ScrollView>        
    </ContentPage.Content>
</ContentPage>